{
  "name": "baffle",
  "description": "Microsub bridge.",
  "version": "0.0.1",
  "private": true,
  "license": "Public domain",
  "author": "Ryan Barrett",
  "repository": {
    "type": "git",
    "url": "https://github.com/snarfed/baffle.git"
  },
  "engines": {
    "node": "8.x.x"
  },
  "scripts": {
    "deploy": "gcloud app deploy",
    "start": "node app.js",
    "system-test": "ava */*.test.js",
    "app-test": "repo-tools test app",
    "test": "npm run system-test",
    "e2e-test": "repo-tools test deploy"
  },
  "dependencies": {
    "express": "^4.16.3",
    "node-fetch": "^2.1.2"
  },
  "devDependencies": {
    "@google-cloud/nodejs-repo-tools": "^2.3.0",
    "ava": "^0.25.0",
    "nock": "^9.4.1",
    "supertest": "^3.1.0"
  },
  "cloud-repo-tools": {
    "requiresKeyFile": true,
    "requiresProjectId": true
  }
}
